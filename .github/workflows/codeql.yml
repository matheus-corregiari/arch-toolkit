name: CodeQL

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  security-events: write
  contents: read

jobs:
  analyze:
    name: Analyze Java-Kotlin
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup
        uses: ./.github/actions/setup-gradle

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: java-kotlin
          build-mode: manual

      # CodeQL currently supports Kotlin through 2.3.x. Use the latest compatible compiler
      # and dependency ABI only for the instrumented analysis build, regardless of the project
      # catalog versions.
      - name: Select CodeQL-compatible Kotlin
        run: |
          sed -E -i \
            -e 's/jetbrains-kotlin = "[^"]+"/jetbrains-kotlin = "2.3.21"/' \
            -e 's/arch-event-observer = "[^"]+"/arch-event-observer = "2.0.1"/' \
            gradle/libs.versions.toml

      - name: Build public modules
        run: ./gradlew ciBuild

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
