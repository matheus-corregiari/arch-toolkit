version: 2.1

executors:
  android:
    docker:
      - image: cimg/android:2022.06

commands:
  default:
    steps:
      - checkout
      - restore_cache:
          key: jars-{{ checksum "build.gradle" }}

jobs:
  build:
    executor: android
    environment:
      JVM_OPTS: -Xmx3200m
    steps:
      - default
      - run:
          name: Download Dependencies
          command: ./gradlew androidDependencies -PDEVELOPER_ID=$DEVELOPER_ID -PDEVELOPER_NAME=$DEVELOPER_NAME -PDEVELOPER_EMAIL=$DEVELOPER_EMAIL -POSSRH_USERNAME=$OSSRH_USERNAME -POSSRH_PASSWORD=$OSSRH_PASSWORD -Psigning.keyId=$SIGNING_KEYID -Psigning.password=$SIGNING_PASSWORD -Psigning.secretKeyRingFile=$PWD/
      - save_cache:
          paths:
            - ~/.gradle
          key: jars-{{ checksum "build.gradle" }}

  # Maldade Jobs
  lint:
    executor: android
    environment:
      JVM_OPTS: -Xmx3200m
    parameters:
      module:
        default: ""
        type: string
    steps:
      - default
      - run: ./gradlew :<<parameters.module>>:lint -PDEVELOPER_ID=$DEVELOPER_ID -PDEVELOPER_NAME=$DEVELOPER_NAME -PDEVELOPER_EMAIL=$DEVELOPER_EMAIL -POSSRH_USERNAME=$OSSRH_USERNAME -POSSRH_PASSWORD=$OSSRH_PASSWORD -Psigning.keyId=$SIGNING_KEYID -Psigning.password=$SIGNING_PASSWORD
      - run: ./gradlew :<<parameters.module>>:ktLint -PDEVELOPER_ID=$DEVELOPER_ID -PDEVELOPER_NAME=$DEVELOPER_NAME -PDEVELOPER_EMAIL=$DEVELOPER_EMAIL -POSSRH_USERNAME=$OSSRH_USERNAME -POSSRH_PASSWORD=$OSSRH_PASSWORD -Psigning.keyId=$SIGNING_KEYID -Psigning.password=$SIGNING_PASSWORD

  test:
    executor: android
    environment:
      JVM_OPTS: -Xmx3200m
    parameters:
      module:
        default: ""
        type: string
    steps:
      - default
      - run: ./gradlew :<<parameters.module>>:testDebugUnitTest -PDEVELOPER_ID=$DEVELOPER_ID -PDEVELOPER_NAME=$DEVELOPER_NAME -PDEVELOPER_EMAIL=$DEVELOPER_EMAIL -POSSRH_USERNAME=$OSSRH_USERNAME -POSSRH_PASSWORD=$OSSRH_PASSWORD -Psigning.keyId=$SIGNING_KEYID -Psigning.password=$SIGNING_PASSWORD

  deploy:
    executor: android
    environment:
      JVM_OPTS: -Xmx3200m
    parameters:
      module:
        default: ""
        type: string
    steps:
      - default
      - run: ./gradlew :<<parameters.module>>:assembleRelease -PDEVELOPER_ID=$DEVELOPER_ID -PDEVELOPER_NAME=$DEVELOPER_NAME -PDEVELOPER_EMAIL=$DEVELOPER_EMAIL -POSSRH_USERNAME=$OSSRH_USERNAME -POSSRH_PASSWORD=$OSSRH_PASSWORD -Psigning.keyId=$SIGNING_KEYID -Psigning.password=$SIGNING_PASSWORD
      - run: ./gradlew :<<parameters.module>>:publishRelease -PDEVELOPER_ID=$DEVELOPER_ID -PDEVELOPER_NAME=$DEVELOPER_NAME -PDEVELOPER_EMAIL=$DEVELOPER_EMAIL -POSSRH_USERNAME=$OSSRH_USERNAME -POSSRH_PASSWORD=$OSSRH_PASSWORD -Psigning.keyId=$SIGNING_KEYID -Psigning.password=$SIGNING_PASSWORD

workflows:
  pull_request:
    jobs:
      # Builds Project and saves build cache
      - build

      # Runs android lint and KtLint
      - lint:
          matrix:
            alias: lint-all-modules
            parameters:
              module:
                - toolkit:delegate
                - toolkit:foldable
                - toolkit:livedata
                - toolkit:recycler-adapter
                - toolkit:statemachine
          name: Lint <<matrix.module>>
          requires:
            - build

      # Runs Unit tests
      - test:
          matrix:
            alias: unit-test-all-modules
            parameters:
              module:
                - toolkit:delegate
                - toolkit:foldable
                - toolkit:livedata
                - toolkit:recycler-adapter
                - toolkit:statemachine
          name: Test <<matrix.module>>
          requires:
            - Lint <<matrix.module>>

      # Ask for approval to run deploy
      - hold-deploy:
          type: approval
          requires:
            - unit-test-all-modules
          filters:
            branches:
              only:
                - master

      # FIXME Run all module in parallel is a problem to sonatype =(
      #      # Deploys all modules
      #      - deploy:
      #          matrix:
      #            alias: deploy-all-modules
      #            parameters:
      #              module:
      #                - toolkit:delegate
      #                - toolkit:foldable
      #                - toolkit:livedata
      #                - toolkit:recycler-adapter
      #                - toolkit:statemachine
      #                - toolkit:foldable
      #          name: Deploy <<matrix.module>>
      #          requires:
      #            - hold-deploy

      # Deploys each modules
      - deploy:
          matrix:
            alias: deploy-delegate
            parameters:
              module:
                - toolkit:delegate
          name: Deploy <<matrix.module>>
          requires:
            - hold-deploy
      - deploy:
          matrix:
            alias: deploy-livedata
            parameters:
              module:
                - toolkit:delegate
          name: Deploy <<matrix.module>>
          requires:
            - deploy-delegate
      - deploy:
          matrix:
            alias: deploy-recycler-adapter
            parameters:
              module:
                - toolkit:delegate
          name: Deploy <<matrix.module>>
          requires:
            - deploy-livedata
      - deploy:
          matrix:
            alias: deploy-statemachine
            parameters:
              module:
                - toolkit:delegate
          name: Deploy <<matrix.module>>
          requires:
            - deploy-recycler-adapter
      - deploy:
          matrix:
            alias: deploy-foldable
            parameters:
              module:
                - toolkit:delegate
          name: Deploy <<matrix.module>>
          requires:
            - deploy-statemachine
