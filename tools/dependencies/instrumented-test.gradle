afterEvaluate {
    if (!project.hasProperty('android')) return

    android {
        dependencies {

            androidTestImplementation libraries.jetbrains.stdlib.jdk8
            androidTestImplementation libraries.jetbrains.test.reflect

            androidTestImplementation libraries.junit.test

            androidTestImplementation libraries.mockito.test.core
            androidTestImplementation libraries.mockito.test.android

            androidTestImplementation libraries.androidx.test.junit
            androidTestImplementation libraries.androidx.test.core
            androidTestImplementation libraries.androidx.annotation
            androidTestImplementation libraries.androidx.appcompat

            androidTestImplementation libraries.android.test.runner
            androidTestImplementation libraries.android.test.rules
            androidTestImplementation libraries.android.test.espresso.core
            androidTestImplementation libraries.android.test.espresso.contrib
            androidTestImplementation libraries.android.test.espresso.intents
            androidTestImplementation libraries.android.test.uiAutomator

            androidTestImplementation libraries.hamcrest.test.core
            androidTestImplementation libraries.hamcrest.test.library

            androidTestImplementation libraries.mirror.test
        }
    }
}